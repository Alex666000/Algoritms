// https://interview.cups.online/live-coding/?room=6d002195-f1d5-4538-b9cf-69ddc684fbc6 password: SSLLIIMM666s -- js код в браузере
// реакт код в браузере: https://stackblitz.com/edit/vitejs-vite-utgzjz?embed=1&file=src%2Fmain.tsx&hideExplorer=1
// исходник реакта:
/*
import { useRef, useState, useEffect } from 'react';
import { createRoot } from 'react-dom/client';

export default function App() {
    const [isVisible, setIsVisible] = useState(false);

    const toggleInput = () => {
        setIsVisible(true);
    };

    return (
        <div>
            <button onClick={toggleInput}>Show and focus input</button>
            {isVisible && <input ref={el => el?.focus()} type="text" />}
        </div>
    );
}

// Создается Реакт приложение
const root = document.getElementById('root');
if (root) {
    createRoot(root).render(<App />);
}
 */
