/*
Что такое атрибут name?
Атрибут name — это стандартный HTML-атрибут, который задается для элементов формы, таких как <input>, <textarea>, <select>, и определяет имя, под которым значение поля будет передано на сервер при отправке формы.

Пример:
<input type="text" name="username" value="Alex" />
При отправке этой формы на сервер будет отправлено: username=Alex

1) Идентификация данных на сервере:
Атрибут "name" используется для обозначения ключа, под которым значение поля передается серверу. Сервер читает данные формы по этим ключам.
Пример:
<input type="text" name="email" value="example@gmail.com" />
Данные формы:
{
  "email": "example@gmail.com"
}

Сбор данных с помощью FormData:

Если используется JavaScript для работы с формой (например, через new FormData()), то name позволяет собрать значения всех полей в виде объекта:
javascript
Копировать код
const form = document.querySelector('form');
const formData = new FormData(form);
console.log(Object.fromEntries(formData.entries()));
// { email: "example@gmail.com" }
Группировка данных:

name можно использовать для создания сложных структур, таких как массивы или вложенные объекты. Например:
html
Копировать код
<input type="text" name="users[0][name]" value="Alex" />
<input type="text" name="users[1][name]" value="Kate" />
Сервер получит данные в виде:
json
Копировать код
{
  "users": [
    { "name": "Alex" },
    { "name": "Kate" }
  ]
}
2) Взаимодействие с сервером:
Сервер ориентируется на "name", чтобы знать, как обработать полученные данные. Без этого атрибута данные не передадутся корректно.
Чем отличается id от name?
id: используется для уникальной идентификации элемента на странице и работы с ним через JavaScript или CSS.

Пример:
<input id="username" />
Здесь id нужен только для манипуляций в DOM, он не участвует в отправке данных на сервер.
"name": участвует в передаче данных на сервер. Без него поле ввода игнорируется при отправке формы.
Когда обязательно использовать "name"?
Если данные формы должны быть отправлены на сервер.
Если используется "FormData" для сбора данных с формы.
Если вы хотите отправить массивы или объекты данных, например:

<input name="users[0][name]" />
<input name="users[1][name]" />
Без name значение поля будет просто игнорироваться при отправке формы.

 */
